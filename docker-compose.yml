services:
  colorir:
    build: .
    volumes:
      - ./output:/app/output
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - COLORIR_AI_MODEL=${COLORIR_AI_MODEL:-gpt-image-1}
    # Override CMD to run interactively:
    #   docker compose run colorir gerar "unicornio" --fonte web
